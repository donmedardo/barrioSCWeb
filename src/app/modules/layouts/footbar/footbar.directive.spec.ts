import { FootbarDirective } from './footbar.directive';

describe('FootbarDirective', () => {
  it('should create an instance', () => {
    const directive = new FootbarDirective();
    expect(directive).toBeTruthy();
  });
});
